package enums;

public enum AutoAcceptMode {
    on,
    off;

    /*@Override
    public String toString() {

        switch(this) {
            case ON:
                return "on";
            case OFF:
                return "off";
            default: throw new IllegalArgumentException();
        }
    }*/
}
