package sample;

// needed to export as a runnable .jar which would be impossible with the Main class since it extends Application
public class App {

    public static void main(String[] args) {
        Main.main(args);
    }
}
